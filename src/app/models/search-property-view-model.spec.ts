/* tslint:disable:no-unused-variable */

import { async, inject } from '@angular/core/testing';
import {SearchPropertyViewModel} from './search-property-view-model';

describe('SearchPropertyViewModel', () => {
  it('should create an instance', () => {
    expect(new SearchPropertyViewModel()).toBeTruthy();
  });
});
